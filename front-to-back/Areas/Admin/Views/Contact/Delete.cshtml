@using front_to_back.Models
@model ContactIntroComponent
<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
             <div class="col-md-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                  
                  <p class="card-description">
                    Delete work component
                  </p>

                  <span>@Model.Title</span>

                  <br>
                  <span>@Model.Description</span>
                  <br>
                  <span>@Model.FilePath</span>
                 
                  <form method="post" asp-action="deletecomponent" asp-controller="contact" asp-area="admin" asp-route-id="@Model.Id">
                      <button onclick="confirm('Are you sure')" class="btn btn-danger">
                          Yes
                      </button>
                  </form>

                </div>
              </div>
            </div>
        </div>
    </div>
</div>